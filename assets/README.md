# Portfolio assets

Add optional files here for a production-ready portfolio:

| File | Purpose |
|------|---------|
| `dinesh_raya.pdf` | Resume download on the Home dashboard |
| `profile.jpg` | Optional headshot (falls back to DR monogram if missing) |

`messages.json` is created locally when Formspree is not configured (dev fallback). It is gitignored.
