<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Dinesh Raya — Articles</title>
    <link>https://github.com/Dinesh-raya/portfolio</link>
    <description>Technical write-ups on AI, Python engineering, and software design by Dinesh Raya.</description>
    <language>en-us</language>
    <lastBuildDate>Sun, 31 May 2026 11:15:22 +0000</lastBuildDate>
    <atom:link href="https://github.com/Dinesh-raya/portfolio/assets/feed.xml" rel="self" type="application/rss+xml"/>
    
    <item>
      <title>Building a Resume Screener with NLP and Streamlit</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/resume-screener</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/resume-screener</guid>
      <pubDate>Mon, 25 May 2026 00:00:00 +0000</pubDate>
      <description>How I built a production-ready resume screening tool that extracts skills from PDFs and ranks candidates using TF-IDF and custom skill taxonomies.</description>
      <content:encoded><![CDATA[# Building a Resume Screener with NLP and Streamlit

Resume screening is one of the most time-consuming tasks in hiring. Automating it with NLP doesn't just save hours — it ensures consistent, unbiased evaluation. Here's how I built a resume screener that extracts skills, ranks candidates, and visualizes matches.

## Architecture Overview

The system follows a pipeline architecture: ingest, parse, extract, score, and visualize.

```
PDF Upload → Text Extraction → Skill Extraction → Vectorization → Ranking → Dashboard
```

## PDF Text Extraction

The first challenge is getting clean text from PDFs. Using PyMuPDF (fitz), we extract text page by page:

```python
import fitz

def extract_text_from_pdf(file_bytes: bytes) -&gt; str:
    doc = fitz.open(stream=file_bytes, filetype=&quot;pdf&quot;)
    text = &quot;&quot;
    for page in doc:
        text += page.get_text()
    doc.close()
    return text
```

Key considerations:
- Handle scanned PDFs with OCR fallback
- Preserve section structure (education, experience, skills)
- Clean up encoding artifacts and special characters

## Skill Extraction with TF-IDF

Instead of relying on external APIs, we use a curated skill taxonomy and TF-IDF vectorization for lightweight, local matching:

```python
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity

SKILL_TAXONOMY = {
    &quot;python&quot;: [&quot;python&quot;, &quot;django&quot;, &quot;flask&quot;, &quot;fastapi&quot;],
    &quot;machine_learning&quot;: [&quot;machine learning&quot;, &quot;deep learning&quot;, &quot;tensorflow&quot;, &quot;pytorch&quot;],
    &quot;data_engineering&quot;: [&quot;etl&quot;, &quot;pipeline&quot;, &quot;spark&quot;, &quot;airflow&quot;],
    &quot;devops&quot;: [&quot;docker&quot;, &quot;kubernetes&quot;, &quot;ci/cd&quot;, &quot;jenkins&quot;],
}
```

This approach:
- Works entirely offline — no API keys needed
- Handles synonyms and related terms
- Produces a dense skill vector for each candidate

## Candidate Scoring

Each resume is scored across multiple dimensions:

| Dimension | Weight | Method |
|-----------|--------|--------|
| Skill Match | 40% | TF-IDF cosine similarity to job description |
| Experience | 30% | Years extracted from timeline sections |
| Education | 15% | Degree level and relevance |
| Certifications | 15% | Presence of relevant certs |

The composite score feeds into a ranked leaderboard displayed in Streamlit.

## Streamlit Dashboard

The frontend provides three views:

1. **Upload &amp; Parse**: Drag-and-drop PDF upload with real-time extraction preview
2. **Skill Matrix**: Heatmap showing which skills each candidate possesses
3. **Ranked Leaderboard**: Sorted candidate cards with scores and expandable detail panels

```python
# Streamlit column layout for candidate cards
cols = st.columns(3)
for i, candidate in enumerate(ranked):
    with cols[i % 3]:
        st.metric(candidate[&quot;name&quot;], f&quot;{candidate['score']}/100&quot;)
        st.progress(candidate[&quot;score&quot;] / 100)
```

## Lessons Learned

1. **PDF quality varies wildly**: Some resumes have no extractable text (scanned images)
2. **Skill taxonomy needs regular updates**: Domain-specific terms evolve quickly
3. **Structured extraction outperforms raw parsing**: Detecting sections improves accuracy
4. **TF-IDF is surprisingly effective**: For domain-specific matching, it often matches embedding-based approaches

## Conclusion

Building a resume screener taught me that practical NLP doesn't always require large language models. With careful feature engineering and a well-designed pipeline, you can build production-quality tools using lightweight, local approaches.]]></content:encoded>
    </item>
    <item>
      <title>Designing High-Performance RAG Pipelines</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/rag-pipelines</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/rag-pipelines</guid>
      <pubDate>Mon, 18 May 2026 00:00:00 +0000</pubDate>
      <description>A deep dive into query expansion, multi-vector indexing, re-ranking strategies, and evaluation frameworks for reliable production LLM systems.</description>
      <content:encoded><![CDATA[# Designing High-Performance RAG Pipelines

Retrieval-Augmented Generation (RAG) has become the standard pattern for grounding LLM responses in factual data. But building a RAG pipeline that actually works in production requires much more than stringing together a vector store and a prompt.

## The Problem with Naive RAG

Basic RAG implementations suffer from several critical issues that degrade user experience and trust:

- **Retrieval miss**: The relevant document isn't in the top-k results, causing the model to hallucinate or refuse to answer
- **Context window waste**: Irrelevant chunks consume valuable token space, reducing the quality of generated responses
- **Hallucination**: The model generates plausible but incorrect information when context is ambiguous or incomplete
- **Latency**: Slow retrieval and generation pipelines frustrate users and break conversational flows

## Query Expansion Strategies

Before retrieving documents, expand the user's query to capture more relevant results. This is one of the most impactful improvements you can make.

```python
def expand_query(query: str, n_variants: int = 3) -&gt; list[str]:
    &quot;&quot;&quot;Generate query variants for better retrieval coverage.&quot;&quot;&quot;
    variants = [query]
    # Add synonyms, rephrases, sub-questions
    # LLM-based expansion works best here
    expanded = llm.generate(f&quot;Rephrase this query {n_variants} ways: {query}&quot;)
    variants.extend(parse_variants(expanded))
    return variants
```

Key techniques include:
- **Synonym expansion**: Replace domain terms with alternatives
- **Decomposition**: Break complex questions into sub-queries
- **Hypothetical document generation**: Generate what the ideal answer document would look like

## Multi-Vector Indexing

Instead of embedding entire documents, embed at multiple granularities for maximum retrieval flexibility:

1. **Sentence-level**: Fine-grained matching for specific facts and details
2. **Paragraph-level**: Context preservation for nuanced understanding
3. **Summary-level**: High-level concept matching for topic discovery
4. **Metadata vectors**: Embed document attributes like date, author, and category

## Re-ranking Strategies

After initial retrieval, re-rank results to maximize relevance:

- **Cross-encoder scoring**: More accurate than bi-encoder similarity because it processes query and document together
- **MMR (Maximal Marginal Relevance)**: Balance relevance and diversity to avoid redundant results
- **Metadata filtering**: Apply date ranges, source authority, and content type filters
- **Learned rankers**: Train small models on user click data for domain-specific ranking

## Evaluation Framework

You can't improve what you don't measure. Build a comprehensive evaluation suite:

| Metric | What It Measures | Target |
|--------|------------------|--------|
| Recall@k | Are relevant docs in top-k? | &gt; 0.85 |
| MRR | How early is first relevant result? | &gt; 0.7 |
| Answer Quality | LLM-as-judge accuracy | &gt; 0.9 |
| Latency P95 | End-to-end response time | &lt; 2s |

## Production Considerations

Beyond the core pipeline, production RAG requires:

- **Caching**: Cache frequent queries and their retrieval results
- **Monitoring**: Track retrieval quality metrics in real-time
- **Fallbacks**: Graceful degradation when retrieval fails
- **A/B testing**: Compare pipeline versions with real users

## Conclusion

Production RAG is an engineering discipline, not just a library call. Invest in evaluation, iterate on retrieval quality, and always validate against real user queries. The difference between a demo and a product is in these details.]]></content:encoded>
    </item>
    <item>
      <title>Prompt Engineering Patterns for Reliable LLM Output</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/prompt-engineering</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/prompt-engineering</guid>
      <pubDate>Sun, 10 May 2026 00:00:00 +0000</pubDate>
      <description>A systematic guide to prompt patterns that produce consistent, structured, and reliable outputs from large language models.</description>
      <content:encoded><![CDATA[# Prompt Engineering Patterns for Reliable LLM Output

Prompt engineering is the art and science of crafting inputs that reliably produce desired outputs from LLMs. Without a systematic approach, results are inconsistent, unstructured, and often require multiple retries.

## The Anatomy of a Good Prompt

Every effective prompt contains four elements:

1. **Role**: Define who the model should act as
2. **Context**: Provide relevant background information
3. **Task**: Specify exactly what to do
4. **Constraints**: Set boundaries on the response format and style

```
Role: You are a senior data engineer reviewing code.
Context: This function processes streaming data from Kafka.
Task: Identify potential performance bottlenecks.
Constraints: List exactly 3 issues with code examples.
```

## Pattern 1: Structured Output

When you need machine-parseable output, lock the format explicitly:

```python
def extract_entities(text: str) -&gt; dict:
    prompt = f&quot;&quot;&quot;
    Extract entities from the following text.
    Return ONLY valid JSON with keys: person, organization, location, date.

    Text: {text}

    JSON:
    &quot;&quot;&quot;
    response = llm.generate(prompt)
    return json.loads(response.strip(&quot;```json&quot;).strip(&quot;```&quot;))
```

This pattern works because it constrains the output space, reducing hallucination and variance.

## Pattern 2: Chain-of-Thought Reasoning

For multi-step problems, guide the model through intermediate reasoning:

```
You are a data analyst. Analyze this sales data step by step:
1. Calculate the monthly growth rate for each product
2. Identify products with declining trends
3. Recommend actions for each declining product
4. Summarize expected impact
```

Each step builds on the previous one, producing more accurate final results than asking for the answer directly.

## Pattern 3: Few-Shot Examples

Provide examples of desired input-output pairs before the actual query:

```
Convert natural language to SQL queries.

Example 1:
Input: &quot;Show me all customers who placed orders last month&quot;
Output: SELECT * FROM customers WHERE id IN (SELECT customer_id FROM orders WHERE order_date &gt;= date('now', '-1 month'));

Example 2:
Input: &quot;What is the total revenue by category?&quot;
Output: SELECT category, SUM(revenue) FROM sales GROUP BY category ORDER BY SUM(revenue) DESC;

Now convert:
Input: &quot;Find products that haven't been sold in 90 days&quot;
Output:
```

## Pattern 4: Self-Correction

Ask the model to critique its own output before finalizing:

```python
def generate_with_review(topic: str) -&gt; str:
    draft = llm.generate(f&quot;Write a technical summary of {topic}&quot;)
    review = llm.generate(f&quot;&quot;&quot;
    Review the following text for accuracy and clarity:
    ---
    {draft}
    ---
    List any errors, omissions, or unclear statements.
    Then provide a corrected version.
    &quot;&quot;&quot;)
    return review
```

## Measuring Prompt Quality

Track these metrics across your prompts:

| Metric | Good | Poor |
|--------|------|------|
| Output consistency | Same input → same output 95%+ | High variance |
| Parse success rate | JSON/structured output parses 100% | Frequent format errors |
| First-attempt accuracy | Correct on first try &gt; 80% | Requires multiple retries |
| Token efficiency | Under 80% of context window | Nearing context limit |

## Conclusion

Systematic prompt engineering transforms LLMs from unpredictable generators into reliable tools. Invest in prompt templates, test rigorously, and treat prompts as code — version-controlled, reviewed, and optimized.]]></content:encoded>
    </item>
    <item>
      <title>Data Quality Automation with Python</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/data-quality-automation</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/data-quality-automation</guid>
      <pubDate>Sat, 25 Apr 2026 00:00:00 +0000</pubDate>
      <description>A practical guide to building automated data quality checks using Great Expectations-style patterns with plain Python and Pandas.</description>
      <content:encoded><![CDATA[# Data Quality Automation with Python

Data quality isn't optional — it's the foundation every downstream decision rests on. Automating quality checks catches problems before they propagate, saves debugging time, and builds trust in your data pipelines.

## Why Automate Data Quality?

Manual data validation is slow, inconsistent, and doesn't scale. Automated checks:

- Run on every pipeline execution
- Catch regressions immediately
- Produce consistent, comparable reports
- Free up engineers for higher-value work

## Building a Validation Framework

You don't need a heavy framework. A validation pipeline can be built with plain Python classes:

```python
from dataclasses import dataclass
from typing import Callable, Any

@dataclass
class Check:
    name: str
    func: Callable
    severity: str = &quot;error&quot;  # error, warning, info

class DataValidator:
    def __init__(self):
        self.checks = []

    def add_check(self, check: Check):
        self.checks.append(check)

    def run(self, df) -&gt; list[dict]:
        results = []
        for check in self.checks:
            try:
                passed = check.func(df)
                results.append({
                    &quot;check&quot;: check.name,
                    &quot;passed&quot;: passed,
                    &quot;severity&quot;: check.severity,
                })
            except Exception as e:
                results.append({
                    &quot;check&quot;: check.name,
                    &quot;passed&quot;: False,
                    &quot;severity&quot;: check.severity,
                    &quot;error&quot;: str(e),
                })
        return results
```

## Essential Quality Checks

Here are the checks I include in every pipeline:

### Completeness Checks

Ensure critical columns have no missing values:

```python
def check_completeness(df, columns: list[str], threshold: float = 0.95):
    for col in columns:
        null_ratio = df[col].isnull().mean()
        if null_ratio &gt; (1 - threshold):
            return False
    return True
```

### Uniqueness Checks

Verify primary key columns contain no duplicates:

```python
def check_uniqueness(df, column: str) -&gt; bool:
    return df[column].is_unique
```

### Range Checks

Validate numeric columns fall within expected bounds:

```python
def check_range(df, column: str, min_val=None, max_val=None) -&gt; bool:
    if min_val is not None and df[column].min() &lt; min_val:
        return False
    if max_val is not None and df[column].max() &gt; max_val:
        return False
    return True
```

### Schema Checks

Confirm column names and types match expectations:

```python
def check_schema(df, expected_schema: dict[str, str]) -&gt; bool:
    for col, dtype in expected_schema.items():
        if col not in df.columns:
            return False
        if str(df[col].dtype) != dtype:
            return False
    return True
```

## Reporting Results

Format validation results into a clear report:

```python
def generate_report(results: list[dict]) -&gt; str:
    passed = sum(1 for r in results if r[&quot;passed&quot;])
    total = len(results)
    report = f&quot;# Data Quality Report\n\n&quot;
    report += f&quot;**{passed}/{total} checks passed**\n\n&quot;
    report += &quot;| Check | Status | Severity |\n&quot;
    report += &quot;|-------|--------|----------|\n&quot;
    for r in results:
        status = &quot;✅&quot; if r[&quot;passed&quot;] else &quot;❌&quot;
        report += f&quot;| {r['check']} | {status} | {r['severity']} |\n&quot;
    return report
```

## Integrating into Pipelines

The validator runs at the ingestion stage of every data pipeline:

```
Source → Extract → Validate → Transform → Validate → Load
```

This two-stage validation catches issues early and prevents bad data from reaching downstream consumers.

## Key Takeaways

1. Start with the five essential checks: completeness, uniqueness, range, schema, and freshness
2. Build checks incrementally as you discover data issues
3. Make validation reports accessible to non-technical stakeholders
4. Fail pipelines on critical check failures, warn on non-critical ones
5. Version your validation rules alongside your pipeline code

Automated data quality transforms trust from a hope into a guarantee. Every pipeline deserves it.]]></content:encoded>
    </item>
    <item>
      <title>Containerizing Data Apps with Docker</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/docker-data-apps</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/docker-data-apps</guid>
      <pubDate>Wed, 15 Apr 2026 00:00:00 +0000</pubDate>
      <description>A practical walkthrough of Dockerizing Streamlit and FastAPI applications for reproducible, deployable data projects.</description>
      <content:encoded><![CDATA[# Containerizing Data Apps with Docker

Docker eliminates the &quot;it works on my machine&quot; problem. For data applications with complex dependency chains — Python packages, system libraries, ML models — containerization is essential for reproducibility and deployment.

## Why Containerize Data Apps?

Data applications have unique packaging challenges:

- **Native dependencies**: Libraries like PyMuPDF, NumPy, and Pandas require compiled binaries
- **Model artifacts**: ML models can be gigabytes — they need to be bundled or mounted
- **Environment parity**: Development, staging, and production must use identical dependencies
- **Scaling**: Containers make horizontal scaling with orchestration tools straightforward

## A Production Dockerfile

Here's a Dockerfile for a Streamlit-based data application:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install system dependencies for native packages
RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends \
    build-essential \
    libgl1-mesa-glx \
    libglib2.0-0 \
    &amp;&amp; rm -rf /var/lib/apt/lists/*

# Copy and install Python dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY . .

# Expose Streamlit's default port
EXPOSE 8501

# Health check
HEALTHCHECK --interval=30s --timeout=3s \
    CMD curl -f http://localhost:8501/_stcore/health || exit 1

ENTRYPOINT [&quot;streamlit&quot;, &quot;run&quot;, &quot;app.py&quot;, &quot;--server.port=8501&quot;, &quot;--server.address=0.0.0.0&quot;]
```

## Multi-Stage Builds for ML Apps

When bundling ML models, use multi-stage builds to keep image sizes manageable:

```dockerfile
# Stage 1: Build dependencies
FROM python:3.11-slim AS builder
COPY requirements.txt .
RUN pip install --user --no-cache-dir -r requirements.txt

# Stage 2: Runtime
FROM python:3.11-slim AS runtime
COPY --from=builder /root/.local /root/.local
COPY model.pkl /app/model.pkl
COPY src/ /app/src
ENV PATH=/root/.local/bin:$PATH
```

This pattern reduces final image size by separating build-time and runtime dependencies.

## Docker Compose for Multi-Service Apps

For applications with multiple services — a Streamlit frontend, FastAPI backend, and PostgreSQL database — Docker Compose orchestrates everything:

```yaml
version: &quot;3.9&quot;
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: portfolio
      POSTGRES_USER: app
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://app:${DB_PASSWORD}@db:5432/portfolio

  app:
    build: .
    ports:
      - &quot;8501:8501&quot;
    depends_on:
      - api
    environment:
      API_URL: http://api:8000

volumes:
  postgres_data:
```

## Best Practices

1. **Pin base image versions**: Never use `latest` — specify `python:3.11-slim` precisely
2. **Use `.dockerignore`**: Exclude `__pycache__`, `.git`, `venv`, and test files
3. **Layer caching**: Order RUN commands from least to most frequently changing
4. **Minimize layers**: Combine related RUN commands with `&amp;&amp;`
5. **Non-root user**: Run applications as a non-root user for security
6. **Health checks**: Always define HEALTHCHECK for orchestration tools

## Conclusion

Containerization turns data applications from fragile scripts into robust, deployable products. Docker's learning curve is shallow, but its impact on reproducibility and deployment confidence is profound. Every data project worth building is worth containerizing.]]></content:encoded>
    </item>
    <item>
      <title>Building Production-Grade Streamlit Apps</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/streamlit-guide</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/streamlit-guide</guid>
      <pubDate>Thu, 02 Apr 2026 00:00:00 +0000</pubDate>
      <description>How to inject custom theme overrides, manage session state dynamically, and write responsive layouts that work across desktop and mobile.</description>
      <content:encoded><![CDATA[# Building Production-Grade Streamlit Apps

Streamlit makes it incredibly easy to build data apps, but getting them to look and feel production-ready requires understanding its internals and applying proven patterns.

## Custom Theme Injection

The secret to custom Streamlit themes is injecting CSS variables early in the page lifecycle:

```python
def inject_theme():
    theme_css = &quot;&quot;&quot;
    :root {
        --accent-color: #4F7CFF;
        --card-bg: rgba(16, 24, 38, 0.75);
        --text-color: #F5F7FA;
        --border-color: rgba(79, 124, 255, 0.15);
    }
    &quot;&quot;&quot;
    st.markdown(f&quot;&lt;style&gt;{theme_css}&lt;/style&gt;&quot;, unsafe_allow_html=True)
```

Key principles:
- Use CSS custom properties for easy theme switching
- Inject styles before any component renders
- Support both dark and light modes with separate variable sets
- Use `!important` sparingly to override Streamlit defaults

## Session State Patterns

Always initialize session state with defaults to prevent KeyError exceptions:

```python
def init_state():
    defaults = {
        &quot;theme&quot;: &quot;dark&quot;,
        &quot;page&quot;: &quot;Home&quot;,
        &quot;chat_history&quot;: [],
        &quot;filters&quot;: {&quot;category&quot;: &quot;All&quot;, &quot;sort&quot;: &quot;newest&quot;},
    }
    for key, val in defaults.items():
        if key not in st.session_state:
            st.session_state[key] = val
```

Best practices:
- Initialize all state variables in one place
- Use meaningful names that describe the data
- Avoid storing large objects in session state
- Clean up stale state periodically

## Responsive Layouts

Use Streamlit's column system with responsive breakpoints:

```python
# Desktop: 2 columns, Mobile: stacked
col1, col2 = st.columns([1.1, 0.9], gap=&quot;large&quot;)

# Responsive via CSS media queries
st.markdown(&quot;&quot;&quot;
&lt;style&gt;
@media (max-width: 768px) {
    .block-container { padding: 1rem; }
    [data-testid=&quot;column&quot;] { width: 100% !important; }
}
&lt;/style&gt;
&quot;&quot;&quot;, unsafe_allow_html=True)
```

## Error Boundaries

Wrap components in error boundaries to prevent cascade failures:

```python
import functools
import traceback

def error_boundary(func):
    @functools.wraps(func)
    def wrapper(*args, **kwargs):
        try:
            return func(*args, **kwargs)
        except Exception as e:
            st.error(&quot;Something went wrong loading this section.&quot;)
            with st.expander(&quot;Technical Details&quot;):
                st.code(traceback.format_exc())
    return wrapper
```

This pattern:
- Prevents one broken component from crashing the entire app
- Shows helpful error details for debugging
- Maintains a professional user experience

## Performance Optimization

Streamlit reruns the entire script on every interaction. Optimize with:

- **Caching**: Use `@st.cache_data` for expensive computations
- **Fragment**: Use `@st.fragment` for partial reruns
- **Lazy loading**: Load heavy resources only when needed
- **Session state**: Store computed results to avoid recomputation

## Key Takeaways

1. Inject CSS early in the page lifecycle using `st.markdown`
2. Initialize all session state variables upfront in one function
3. Use error boundaries on every component to isolate failures
4. Test on mobile viewports to ensure responsive layouts work
5. Cache expensive operations to maintain snappy performance

Streamlit's simplicity is its superpower. Work with the framework, not against it, and you can build production-grade applications remarkably quickly.]]></content:encoded>
    </item>
    <item>
      <title>Writing Clean Python: Typing, Linting, and Testing</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/clean-python</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/clean-python</guid>
      <pubDate>Wed, 25 Mar 2026 00:00:00 +0000</pubDate>
      <description>How type hints, automated linting, and a practical testing strategy transform Python code from throwaway scripts into maintainable systems.</description>
      <content:encoded><![CDATA[# Writing Clean Python: Typing, Linting, and Testing

Python's flexibility is a double-edged sword. Without discipline, scripts become unmaintainable quickly. Three practices transform ad-hoc Python into production-grade code: static typing, automated linting, and thorough testing.

## Type Hints: Self-Documenting Contracts

Type hints catch entire categories of bugs at development time and serve as living documentation:

```python
from typing import Optional, List, Dict, Tuple

def process_transactions(
    transactions: List[Dict[str, float]],
    currency: str = &quot;USD&quot;,
    min_amount: Optional[float] = None,
) -&gt; Tuple[float, int]:
    &quot;&quot;&quot;Process a list of transactions and return (total, count).&quot;&quot;&quot;
    filtered = transactions
    if min_amount is not None:
        filtered = [t for t in transactions if t.get(&quot;amount&quot;, 0) &gt;= min_amount]
    total = sum(t.get(&quot;amount&quot;, 0) for t in filtered)
    return total, len(filtered)
```

Key benefits:
- IDEs provide accurate autocompletion and inline error detection
- Refactoring becomes safer — the type checker validates changes
- New team members understand function contracts instantly

Use `mypy` or `pyright` for static type checking:

```bash
mypy src/ --strict
```

## Linting: Automated Code Review

Linters enforce consistency and catch common mistakes automatically. I use `ruff` for speed and `pylint` for depth:

```toml
# pyproject.toml
[tool.ruff]
line-length = 100
select = [&quot;E&quot;, &quot;F&quot;, &quot;I&quot;, &quot;N&quot;, &quot;W&quot;, &quot;PL&quot;, &quot;UP&quot;]

[tool.ruff.per-file-ignores]
&quot;tests/*&quot; = [&quot;PLR0913&quot;]
```

Ruff runs in milliseconds and catches:
- Unused imports and variables
- Inconsistent naming conventions
- Complex functions that need refactoring
- Common security anti-patterns

## Testing Strategy

A practical testing pyramid for data applications:

```
     ╱╲
    ╱ E2E ╲
   ╱───────╲
  ╱ Integration ╲
 ╱────────────────╲
╱   Unit Tests     ╲
╱────────────────────╲
```

### Unit Tests

Test individual functions in isolation:

```python
def test_process_transactions_empty():
    assert process_transactions([]) == (0.0, 0)

def test_process_transactions_filter():
    txns = [{&quot;amount&quot;: 10}, {&quot;amount&quot;: 50}]
    total, count = process_transactions(txns, min_amount=20)
    assert total == 50.0
    assert count == 1
```

### Integration Tests

Test data pipeline stages together:

```python
def test_extract_transform_pipeline():
    raw = extract_source()
    cleaned = clean_data(raw)
    transformed = transform_features(cleaned)
    assert transformed.isnull().sum().sum() == 0
    assert len(transformed.columns) == expected_features
```

### Property-Based Testing

Test invariants that must always hold:

```python
from hypothesis import given, strategies as st

@given(st.lists(st.floats(min_value=0, max_value=1e6)))
def test_aggregation_invariants(values):
    result = aggregate(values)
    assert result[&quot;count&quot;] == len(values)
    assert result[&quot;sum&quot;] &gt;= 0
    assert result[&quot;mean&quot;] == result[&quot;sum&quot;] / result[&quot;count&quot;]
```

## Continuous Integration

Automate all three practices in CI:

```yaml
# .github/workflows/ci.yml
jobs:
  quality:
    steps:
      - run: ruff check .
      - run: mypy src/ --strict
      - run: pytest tests/ --cov=src --cov-fail-under=80
```

This pipeline runs on every pull request, ensuring no degraded code reaches production.

## Practical Adoption Tips

1. **Start with linting**: Ruff is zero-config and catches the most issues
2. **Add types incrementally**: Type critical function signatures first
3. **Test the risky parts**: Focus tests on complex logic and edge cases
4. **Run checks in CI**: Never rely on developers running tools manually
5. **Keep it practical**: 100% test coverage is not the goal — meaningful coverage is

## Conclusion

Clean Python doesn't happen by accident. Type hints, linting, and testing form a feedback loop that catches errors early, documents intent, and makes codebases a joy to work in. Invest in these practices early — the compound interest is enormous.]]></content:encoded>
    </item>
    <item>
      <title>Practical OOP Design Patterns in Python</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/oop-patterns</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/oop-patterns</guid>
      <pubDate>Tue, 10 Mar 2026 00:00:00 +0000</pubDate>
      <description>Learn how Singleton, Factory, and Strategy patterns make automation code modular, testable, and robust.</description>
      <content:encoded><![CDATA[# Practical OOP Design Patterns in Python

Design patterns aren't just academic exercises. When applied judiciously, they solve real problems in automation, scripting, and application code. Here are three patterns I use regularly in production Python.

## The Singleton Pattern

Use when you need exactly one instance of a class throughout your application. Common use cases include configuration managers, database connection pools, and logging handlers.

```python
class Config:
    _instance = None

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
            cls._instance._load()
        return cls._instance

    def _load(self):
        &quot;&quot;&quot;Load configuration from environment and files.&quot;&quot;&quot;
        import os
        self.debug = os.getenv(&quot;DEBUG&quot;, &quot;false&quot;).lower() == &quot;true&quot;
        self.db_url = os.getenv(&quot;DATABASE_URL&quot;, &quot;sqlite:///default.db&quot;)
        self.api_key = os.getenv(&quot;API_KEY&quot;, &quot;&quot;)
```

Benefits:
- Single source of truth for configuration
- Lazy initialization on first access
- Thread-safe with proper locking if needed

Cautions:
- Makes testing harder (state persists between tests)
- Can hide dependencies
- Consider dependency injection as an alternative

## The Factory Pattern

Use when you need to create objects based on runtime conditions. This is especially useful for plugin systems, format handlers, and API client selection.

```python
class ScraperFactory:
    _scrapers = {}

    @classmethod
    def register(cls, source: str, scraper_class):
        cls._scrapers[source] = scraper_class

    @classmethod
    def create(cls, source: str):
        scraper_class = cls._scrapers.get(source)
        if not scraper_class:
            raise ValueError(f&quot;Unknown source: {source}&quot;)
        return scraper_class()

# Registration
ScraperFactory.register(&quot;github&quot;, GitHubScraper)
ScraperFactory.register(&quot;linkedin&quot;, LinkedInScraper)

# Usage
scraper = ScraperFactory.create(&quot;github&quot;)
```

Benefits:
- Decouples creation logic from usage
- Easy to add new types without modifying existing code
- Centralizes configuration and initialization

## The Strategy Pattern

Use when you need interchangeable algorithms or behaviors. This pattern shines for export formats, validation rules, and pricing models.

```python
class DataExporter:
    def __init__(self, strategy):
        self.strategy = strategy

    def export(self, data):
        return self.strategy(data)

# Strategies as simple functions
def csv_strategy(data):
    import pandas as pd
    return pd.DataFrame(data).to_csv(index=False)

def json_strategy(data):
    import json
    return json.dumps(data, indent=2, default=str)

def markdown_strategy(data):
    headers = data[0].keys()
    lines = [&quot;| &quot; + &quot; | &quot;.join(headers) + &quot; |&quot;]
    lines.append(&quot;| &quot; + &quot; | &quot;.join([&quot;---&quot;] * len(headers)) + &quot; |&quot;)
    for row in data:
        lines.append(&quot;| &quot; + &quot; | &quot;.join(str(row[h]) for h in headers) + &quot; |&quot;)
    return &quot;\n&quot;.join(lines)

# Usage
exporter = DataExporter(csv_strategy)
print(exporter.export(records))
```

Benefits:
- Algorithms are first-class objects
- Easy to swap behaviors at runtime
- Each strategy is independently testable

## When to Use Patterns

| Pattern | Use When | Avoid When |
|---------|----------|------------|
| Singleton | Global config, logging, connection pools | You need multiple instances or easy testing |
| Factory | Plugin systems, format handlers, conditional creation | Only 1-2 types that never change |
| Strategy | Export formats, validation rules, sorting algorithms | Algorithm rarely changes |

## Key Insight

Patterns should emerge from problems, not be applied preemptively. Start with the simplest solution that works. When complexity demands it, refactor to patterns. Premature abstraction is worse than no abstraction.

The best code is code that clearly communicates intent. If a pattern makes your code harder to read, it's the wrong pattern for that situation.]]></content:encoded>
    </item>
    <item>
      <title>SQL Essentials for Data Engineers</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/sql-data-engineering</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/sql-data-engineering</guid>
      <pubDate>Sun, 01 Mar 2026 00:00:00 +0000</pubDate>
      <description>Beyond basic SELECT queries — window functions, CTEs, query optimization, and patterns that separate production data engineers from casual users.</description>
      <content:encoded><![CDATA[# SQL Essentials for Data Engineers

SQL is the lingua franca of data engineering. While basic queries get you started, production data work demands deeper knowledge of window functions, Common Table Expressions, query optimization, and robust pipeline patterns.

## Window Functions: Analytics Without Self-Joins

Window functions perform calculations across rows related to the current row without collapsing the result set:

```sql
SELECT
    department,
    employee_name,
    salary,
    RANK() OVER (PARTITION BY department ORDER BY salary DESC) as dept_rank,
    AVG(salary) OVER (PARTITION BY department) as dept_avg,
    salary - AVG(salary) OVER (PARTITION BY department) as above_avg
FROM employees;
```

Key window functions:
- **ROW_NUMBER()**: Sequential row numbering within partitions
- **RANK() / DENSE_RANK()**: Ranking with gaps (RANK) or without (DENSE_RANK)
- **LAG() / LEAD()**: Access previous or next row values
- **SUM() OVER**: Running totals and moving averages

## Common Table Expressions: Modular Queries

CTEs make complex queries readable and debuggable by breaking them into named steps:

```sql
WITH monthly_revenue AS (
    SELECT
        DATE_TRUNC('month', order_date) as month,
        SUM(amount) as revenue
    FROM orders
    WHERE status = 'completed'
    GROUP BY 1
),
growth AS (
    SELECT
        month,
        revenue,
        LAG(revenue) OVER (ORDER BY month) as prev_revenue,
        ROUND(
            (revenue - LAG(revenue) OVER (ORDER BY month)) /
            NULLIF(LAG(revenue) OVER (ORDER BY month), 0) * 100,
            2
        ) as growth_pct
    FROM monthly_revenue
)
SELECT * FROM growth
WHERE prev_revenue IS NOT NULL
ORDER BY growth_pct DESC;
```

## Query Optimization Patterns

Slow queries are the most common production issue. These patterns consistently improve performance:

### Filter Early, Filter Often

Apply WHERE clauses before joins to reduce the data volume entering expensive operations:

```sql
-- Slow: filters after join
SELECT *
FROM customers c
JOIN orders o ON c.id = o.customer_id
WHERE o.order_date &gt;= '2026-01-01';

-- Fast: filters before join
SELECT *
FROM customers c
JOIN (SELECT * FROM orders WHERE order_date &gt;= '2026-01-01') o
    ON c.id = o.customer_id;
```

### Indexing Strategy

| Query Pattern | Index Type | Example |
|--------------|-----------|---------|
| Exact lookup | B-tree | `WHERE id = 42` |
| Range queries | B-tree (sorted) | `WHERE date &gt; '2026-01-01'` |
| Text search | GIN/trigram | `WHERE name ILIKE '%search%'` |
| JSON queries | GIN | `WHERE metadata-&gt;&gt;'key' = 'value'` |

### EXPLAIN ANALYZE

Always profile queries before optimizing:

```sql
EXPLAIN ANALYZE
SELECT customer_id, COUNT(*)
FROM orders
WHERE status = 'pending'
GROUP BY customer_id;
```

Look for sequential scans on large tables, high row estimates vs actuals, and sorts spilling to disk.

## Pipeline Patterns

### Incremental Loading

Instead of full reloads, process only changed records:

```sql
-- Using a watermark table
INSERT INTO analytics.daily_orders
SELECT *
FROM raw.orders
WHERE updated_at &gt; (SELECT last_processed FROM pipeline_watermark WHERE table_name = 'orders');
```

### Idempotent Upserts

Ensure pipelines can be re-run safely:

```sql
MERGE INTO target_table AS t
USING source_table AS s
ON t.id = s.id
WHEN MATCHED THEN UPDATE SET
    t.value = s.value,
    t.updated_at = CURRENT_TIMESTAMP
WHEN NOT MATCHED THEN INSERT (id, value, created_at)
VALUES (s.id, s.value, CURRENT_TIMESTAMP);
```

## Conclusion

SQL mastery separates data engineers who can ship reliable pipelines from those who write fragile, slow queries. Invest in window functions, CTEs, and optimization patterns — they pay dividends on every project.]]></content:encoded>
    </item>
    <item>
      <title>Building REST APIs with FastAPI</title>
      <link>https://github.com/Dinesh-raya/portfolio/articles/fastapi-apis</link>
      <guid>https://github.com/Dinesh-raya/portfolio/articles/fastapi-apis</guid>
      <pubDate>Sun, 15 Feb 2026 00:00:00 +0000</pubDate>
      <description>A production-focused guide to FastAPI: Pydantic validation, dependency injection, async endpoints, and deployment patterns.</description>
      <content:encoded><![CDATA[# Building REST APIs with FastAPI

FastAPI has become my go-to framework for building Python APIs. It combines automatic OpenAPI documentation, Pydantic validation, async support, and excellent performance — all with minimal boilerplate.

## Why FastAPI?

- **Automatic validation**: Pydantic models define request/response schemas
- **Self-documenting**: OpenAPI and ReDoc documentation generated automatically
- **Async by default**: Native support for asynchronous endpoints
- **Dependency injection**: Clean separation of concerns without heavy frameworks
- **Performance**: On par with Node.js and Go for typical API workloads

## Project Structure

A maintainable FastAPI project follows a modular structure:

```
app/
├── main.py          # Application entry point
├── config.py        # Settings and configuration
├── models/          # Pydantic schemas
├── routers/         # Endpoint definitions
├── services/        # Business logic
└── dependencies/    # Reusable dependencies
```

## Defining Models with Pydantic

Pydantic V2 provides runtime validation with near-zero overhead:

```python
from pydantic import BaseModel, Field, EmailStr
from datetime import datetime
from typing import Optional

class ContactMessage(BaseModel):
    name: str = Field(..., min_length=1, max_length=100)
    email: EmailStr
    subject: str = Field(default=&quot;&quot;, max_length=200)
    message: str = Field(..., min_length=10, max_length=5000)
    created_at: datetime = Field(default_factory=datetime.utcnow)

class ContactResponse(BaseModel):
    success: bool
    message: str
    message_id: Optional[str] = None
```

## Endpoints with Dependency Injection

Dependencies keep endpoints thin and testable:

```python
from fastapi import FastAPI, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

app = FastAPI(title=&quot;Portfolio API&quot;, version=&quot;1.0.0&quot;)

async def get_db() -&gt; AsyncSession:
    async with async_session() as session:
        yield session

@app.post(&quot;/api/contact&quot;, response_model=ContactResponse)
async def submit_contact(
    message: ContactMessage,
    db: AsyncSession = Depends(get_db),
):
    try:
        message_id = await contact_service.save_message(db, message)
        await email_service.notify(message)
        return ContactResponse(success=True, message=&quot;Message sent&quot;, message_id=message_id)
    except Exception as e:
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail=str(e),
        )
```

## Async Database Integration

FastAPI pairs naturally with async database drivers:

```python
from sqlalchemy import select, func
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession

class ContactService:
    def __init__(self, db_url: str):
        self.engine = create_async_engine(db_url)

    async def save_message(self, db: AsyncSession, message: ContactMessage) -&gt; str:
        stmt = contact_table.insert().values(
            name=message.name,
            email=message.email,
            subject=message.subject,
            message=message.message,
        ).returning(contact_table.c.id)
        result = await db.execute(stmt)
        await db.commit()
        return str(result.scalar_one())
```

## Error Handling

Consistent error responses improve API usability:

```python
from fastapi import Request
from fastapi.responses import JSONResponse

@app.exception_handler(ValueError)
async def value_error_handler(request: Request, exc: ValueError):
    return JSONResponse(
        status_code=400,
        content={&quot;success&quot;: False, &quot;error&quot;: str(exc)},
    )

@app.exception_handler(Exception)
async def general_error_handler(request: Request, exc: Exception):
    return JSONResponse(
        status_code=500,
        content={&quot;success&quot;: False, &quot;error&quot;: &quot;An unexpected error occurred&quot;},
    )
```

## Deployment

Deploy FastAPI with production-ready configuration:

```python
# main.py
import uvicorn

if __name__ == &quot;__main__&quot;:
    uvicorn.run(
        &quot;app.main:app&quot;,
        host=&quot;0.0.0.0&quot;,
        port=8000,
        reload=False,
        workers=4,
        log_level=&quot;info&quot;,
    )
```

And serve with a reverse proxy:

```nginx
server {
    listen 80;
    server_name api.example.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## Key Takeaways

1. Start with Pydantic models — they define your API contract
2. Use dependency injection for database sessions, authentication, and configuration
3. Write async endpoints for I/O-bound operations
4. Handle errors consistently with custom exception handlers
5. Always include health check and metrics endpoints in production

FastAPI's combination of modern Python features, automatic documentation, and excellent performance makes it the strongest choice for data-focused API development today.]]></content:encoded>
    </item>
  </channel>
</rss>