# Copy to .streamlit/secrets.toml for local development (do not commit real IDs).
# On Streamlit Cloud: App settings → Secrets → paste the same structure.
#
# The contact section uses a direct email link (mailto:) —
# no external services needed. Add secrets here as needed.